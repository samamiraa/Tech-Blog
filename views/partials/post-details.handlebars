<div class="card m-3" data-bs-theme="dark">
    <a class="text-light" href="/api/post/{{id}}">
        <div class="card-header">
        {{title}}
        </div>

        <div class="card-body">
            {{description}}
        </div>

        <div class="card-footer">
            by {{user.firstName}} on {{format_date createdAt}}
        </div>
    </a>
</div>